# ╔══════════════════════════════════════════════════════════════╗
# ║  WD Portal API — Bootstrap Properties                        ║
# ║  This file only exists to satisfy Spring Boot's property     ║
# ║  loader. ALL real config lives in application.yml and        ║
# ║  application-{local|staging|production}.yml                  ║
# ║                                                              ║
# ║  ACTIVATE profile at runtime:                                ║
# ║    SPRING_PROFILES_ACTIVE=production                         ║
# ║    (set by PM2 env on VPS, or IDE run config locally)        ║
# ╚══════════════════════════════════════════════════════════════╝

# ⚠️ DO NOT add .env imports — env vars belong in profile YAMLs
# spring.config.import removed intentionally
spring.application.name=wd-portal-api